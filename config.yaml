target_repo: openclaw/openclaw
fork_repo: arosstale/openclaw
fork_remote: fork
fork_path: /tmp/openclaw-fork
gates:
  build: pnpm build
  check: pnpm check
  test: pnpm test
  format: npx oxfmt --write
known_flakes:
  - "checks-windows (test)"
  - "checks (bun, test)"
  - "checks (node, test)"
